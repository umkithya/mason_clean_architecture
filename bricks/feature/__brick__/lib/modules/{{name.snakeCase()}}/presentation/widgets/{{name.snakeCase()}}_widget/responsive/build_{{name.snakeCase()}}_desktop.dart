import 'package:flutter/material.dart';

class Build{{name.pascalCase()}}Desktop extends StatelessWidget {
  const Build{{name.pascalCase()}}Desktop ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Desktop"));
  }
}