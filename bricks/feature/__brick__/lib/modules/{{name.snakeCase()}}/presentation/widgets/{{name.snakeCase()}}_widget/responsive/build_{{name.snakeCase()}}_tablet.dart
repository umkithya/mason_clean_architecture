import 'package:flutter/material.dart';

class Build{{name.pascalCase()}}Tablet extends StatelessWidget {
  const Build{{name.pascalCase()}}Tablet ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Tablet"));
  }
}