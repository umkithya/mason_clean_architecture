import 'package:flutter/material.dart';

class Build{{name.pascalCase()}}Mobile extends StatelessWidget {
  const Build{{name.pascalCase()}}Mobile ({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Mobile"));
  }
}